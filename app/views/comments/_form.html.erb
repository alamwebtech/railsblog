<%= form_for ([@post, @post.comments.build]) do |f| %>
<p id="nameLabel">
  <%= f.label :name %> <br>
  <%= f.text_field :name %>
</p>
<p id="commentLabel">
  <%= f.label :body %> <br>
  <%= f.text_area :body %>
</P> <br>

<p id="create">
  <%= f.submit %>
</p>

<% end %>
